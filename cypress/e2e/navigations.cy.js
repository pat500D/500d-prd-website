/// <reference types = "cypress" />

describe('Navigations', () => {

    beforeEach(() => {
        cy.visit('/')
        cy.getCookies() 
    });
    
    it('1. Check header navigations', () => {
        
    });

    it('2. Check footer navigations', () => {
        
    });
});